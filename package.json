{
  "name": "exponential-imagination-video-backend",
  "version": "1.0.0",
  "description": "Backend proxy: turns script scenes into xAI video-generation prompts, generates each clip, and stitches them into one final video. Keeps the xAI API key server-side.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ffmpeg-static": "^5.2.0",
    "fluent-ffmpeg": "^2.1.3"
  }
}
